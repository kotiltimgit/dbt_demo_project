version: 2

sources:
  - name: DBT_API_METADATA
    database: "{{ env_var('DBT_ENV_DB') }}"
    schema: DBT_API_METADATA
    tables:
      - name: DBT_ACCOUNTS
      - name: DBT_PROJECTS
      - name: DBT_ENVIRONMENTS
      - name: DBT_JOBS
      - name: DBT_USERS

        